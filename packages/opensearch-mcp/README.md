# opensearch-mcp

Zero-config `web_search` and `web_fetch` for any MCP client.

```json
{
  "mcpServers": {
    "opensearch": { "command": "npx", "args": ["-y", "opensearch-mcp"] }
  }
}
```

## License

MIT
